<!-- Generated by npm run generate:commands. Do not edit. -->

# jurisd command reference

This reference is generated from the command contract registry.

Supported completion shells: `bash`, `zsh`, `fish`.

## Command summary

| Command                 | Group     | Side effect           | MCP                     | Summary                                                    |
| ----------------------- | --------- | --------------------- | ----------------------- | ---------------------------------------------------------- |
| `bibliography`          | `cite`    | `local_metadata_read` | `bibliography`          | Render a bibliography from cached citations.               |
| `cache-cited-by`        | `cite`    | `network_read`        | `cache_cited_by`        | Cache cited-by information for a citation key.             |
| `cite`                  | `cite`    | `local_metadata_read` | `cite`                  | Create or record a citation cache entry.                   |
| `completion`            | `doctor`  | `read_only_query`     | CLI only                | Print a shell completion script.                           |
| `fetch-document-text`   | `source`  | `network_read`        | `fetch_document_text`   | Fetch full text for a source document.                     |
| `fetch-module`          | `modules` | `filesystem_write`    | CLI only                | Fetch and install a data module.                           |
| `find-citing`           | `graph`   | `local_metadata_read` | `find_citing`           | Find locally indexed items citing or considering a target. |
| `format-citation`       | `cite`    | `read_only_query`     | `format_citation`       | Format an AGLC4 citation.                                  |
| `get-act-structure`     | `corpus`  | `local_metadata_read` | `get_act_structure`     | Get act structure from an installed local data module.     |
| `get-provision`         | `corpus`  | `local_metadata_read` | `get_provision`         | Get a provision from an installed local data module.       |
| `source-lookup`           | `source`  | `network_read`        | `source_lookup`           | Look up removed.invalid article metadata or citation URL.          |
| `list-data-modules`     | `corpus`  | `local_metadata_read` | `list_data_modules`     | List installed local data modules.                         |
| `list-modules`          | `modules` | `local_metadata_read` | CLI only                | List installed data modules via the operator CLI.          |
| `resolve-citation`      | `cite`    | `network_read`        | `resolve_citation`      | Resolve a citation to an authoritative source.             |
| `search-cases`          | `search`  | `network_read`        | `search_cases`          | Search Australian and New Zealand case law.                |
| `search-citing-cases`   | `cite`    | `network_read`        | `search_citing_cases`   | Search for cases citing a named case.                      |
| `search-legislation`    | `search`  | `network_read`        | `search_legislation`    | Search Australian and New Zealand legislation.             |
| `semantic-search-local` | `search`  | `local_metadata_read` | `semantic_search_local` | Run local semantic search over installed data modules.     |
| `verify-module`         | `modules` | `local_metadata_read` | CLI only                | Verify an installed data module.                           |

## `bibliography`

Render a bibliography from cached citations.

- Command id: `cite.bibliography`
- Group: `cite`
- Side effect: `local_metadata_read`
- Stability: `stable`
- MCP tool: `bibliography`
- Result contract: `cite.bibliography.v1`

Usage:

```bash
jurisd bibliography
```

Arguments:

- none

Flags:

- `--op` (string): Bibliography operation.
- `--query` (string): Cached citation lookup query.
- `--cite-key` (string): Cached citation key.
- `--document` (string): Logical document name.
- `--format` (string): Output format.
- `--output-path` (string): BibLaTeX output path.

Examples:

- `jurisd bibliography`

## `cache-cited-by`

Cache cited-by information for a citation key.

- Command id: `cite.cacheCitedBy`
- Group: `cite`
- Side effect: `network_read`
- Stability: `stable`
- MCP tool: `cache_cited_by`
- Result contract: `cite.cacheCitedBy.v1`

Usage:

```bash
jurisd cache-cited-by mabo-1992-hca-23
```

Arguments:

- `citeKey`: citeKey argument.

Flags:

- none

Examples:

- `jurisd cache-cited-by mabo-1992-hca-23`

## `cite`

Create or record a citation cache entry.

- Command id: `cite.create`
- Group: `cite`
- Side effect: `local_metadata_read`
- Stability: `stable`
- MCP tool: `cite`
- Result contract: `cite.create.v1`

Usage:

```bash
jurisd cite 'Mabo v Queensland (No 2)' --year 1992
```

Arguments:

- `title`: title argument.

Flags:

- `--year` (number): year value.
- `--footnote-number` (number): footnoteNumber value.
- `--keywords` (array): keywords values.
- `--action` (string): Citation cache action.
- `--neutral-citation` (string): Neutral citation.
- `--reported-citation` (string): Reported citation.
- `--url` (string): Primary source URL.
- `--type` (string): Source type.
- `--jurisdiction` (string): Jurisdiction code.
- `--court` (string): Court code.
- `--summary` (string): Brief source summary.
- `--document` (string): Logical document name.
- `--pinpoint` (string): Pinpoint reference.
- `--style` (string): Citation style.
- `--cite-key` (string): Cached citation key.

Examples:

- `jurisd cite 'Mabo v Queensland (No 2)' --year 1992`

## `completion`

Print a shell completion script.

- Command id: `shell.completion`
- Group: `doctor`
- Side effect: `read_only_query`
- Stability: `stable`
- MCP tool: not exposed
- Result contract: `shell.completion.v1`

Usage:

```bash
jurisd completion <bash|zsh|fish>
```

Arguments:

- `shell`: shell argument.

Flags:

- none

Examples:

- `jurisd completion <bash|zsh|fish>`

## `fetch-document-text`

Fetch full text for a source document.

- Command id: `source.fetchDocument`
- Group: `source`
- Side effect: `network_read`
- Stability: `stable`
- MCP tool: `fetch_document_text`
- Result contract: `source.fetchDocument.v1`

Usage:

```bash
jurisd fetch-document-text <url>
```

Arguments:

- `url`: url argument.

Flags:

- `--format` (string): Output format.
- `--cite-key` (string): Existing citation cache key to associate with fetched source metadata.

Examples:

- `jurisd fetch-document-text <url>`

## `fetch-module`

Fetch and install a data module.

- Command id: `modules.fetch`
- Group: `modules`
- Side effect: `filesystem_write`
- Stability: `stable`
- MCP tool: not exposed
- Result contract: `modules.fetch.v1`

Usage:

```bash
jurisd fetch-module <name> [--manifest-url URL] [--modules-dir DIR]
```

Arguments:

- `name`: name argument.

Flags:

- `--manifest-url` (string): Override manifest URL.
- `--modules-dir` (string): Override modules directory.

Examples:

- `jurisd fetch-module <name> [--manifest-url URL] [--modules-dir DIR]`

## `find-citing`

Find locally indexed items citing or considering a target.

- Command id: `graph.findCiting`
- Group: `graph`
- Side effect: `local_metadata_read`
- Stability: `stable`
- MCP tool: `find_citing`
- Result contract: `graph.findCiting.v1`

Usage:

```bash
jurisd find-citing '[1992] HCA 23' --kinds cites,considers
```

Arguments:

- `target`: target argument.

Flags:

- `--limit` (number): limit value.
- `--kinds` (array): kinds values.
- `--module` (string): Pinned data module name.
- `--format` (string): Output format.

Examples:

- `jurisd find-citing '[1992] HCA 23' --kinds cites,considers`

## `format-citation`

Format an AGLC4 citation.

- Command id: `cite.format`
- Group: `cite`
- Side effect: `read_only_query`
- Stability: `stable`
- MCP tool: `format_citation`
- Result contract: `cite.format.v1`

Usage:

```bash
jurisd format-citation 'Mabo v Queensland (No 2)' --neutral-citation '[1992] HCA 23'
```

Arguments:

- `title`: title argument.

Flags:

- `--footnote-ref` (number): footnoteRef value.
- `--pinpoint-para` (number): pinpointPara value.
- `--pinpoint-page` (number): pinpointPage value.
- `--paragraph-number` (number): paragraphNumber value.
- `--mode` (string): Citation mode.
- `--neutral-citation` (string): Neutral citation.
- `--reported-citation` (string): Reported citation.
- `--pinpoint` (string): Pinpoint reference.
- `--style` (string): Citation style.
- `--url` (string): AustLII document URL for pinpoint mode.
- `--phrase` (string): Phrase to locate for pinpoint mode.
- `--case-citation` (string): Case citation prefix for pinpoint mode.

Examples:

- `jurisd format-citation 'Mabo v Queensland (No 2)' --neutral-citation '[1992] HCA 23'`

## `get-act-structure`

Get act structure from an installed local data module.

- Command id: `corpus.getActStructure`
- Group: `corpus`
- Side effect: `local_metadata_read`
- Stability: `stable`
- MCP tool: `get_act_structure`
- Result contract: `corpus.getActStructure.v1`

Usage:

```bash
jurisd get-act-structure 'Family Law Act 1975 (Cth)'
```

Arguments:

- `act`: act argument.

Flags:

- `--depth` (number): depth value.
- `--module` (string): Pinned data module name.
- `--format` (string): Output format.

Examples:

- `jurisd get-act-structure 'Family Law Act 1975 (Cth)'`

## `get-provision`

Get a provision from an installed local data module.

- Command id: `corpus.getProvision`
- Group: `corpus`
- Side effect: `local_metadata_read`
- Stability: `stable`
- MCP tool: `get_provision`
- Result contract: `corpus.getProvision.v1`

Usage:

```bash
jurisd get-provision 'Family Law Act 1975 (Cth)' 's 60CC'
```

Arguments:

- `act`: act argument.
- `provision`: provision argument.

Flags:

- `--module` (string): Pinned data module name.
- `--format` (string): Output format.

Examples:

- `jurisd get-provision 'Family Law Act 1975 (Cth)' 's 60CC'`

## `source-lookup`

Look up removed.invalid article metadata or citation URL.

- Command id: `source.sourceLookup`
- Group: `source`
- Side effect: `network_read`
- Stability: `stable`
- MCP tool: `source_lookup`
- Result contract: `source.sourceLookup.v1`

Usage:

```bash
jurisd source-lookup --by citation --citation '[2008] NSWSC 323'
```

Arguments:

- none

Flags:

- `--article-id` (number): articleId value.
- `--by` (string): Lookup mode.
- `--citation` (string): Neutral citation for by=citation lookups.

Examples:

- `jurisd source-lookup --by citation --citation '[2008] NSWSC 323'`

## `list-data-modules`

List installed local data modules.

- Command id: `corpus.listDataModules`
- Group: `corpus`
- Side effect: `local_metadata_read`
- Stability: `stable`
- MCP tool: `list_data_modules`
- Result contract: `corpus.listDataModules.v1`

Usage:

```bash
jurisd list-data-modules --include-invalid true
```

Arguments:

- none

Flags:

- `--refresh` (boolean): refresh toggle.
- `--include-invalid` (boolean): includeInvalid toggle.
- `--format` (string): Output format.

Examples:

- `jurisd list-data-modules --include-invalid true`

## `list-modules`

List installed data modules via the operator CLI.

- Command id: `modules.list`
- Group: `modules`
- Side effect: `local_metadata_read`
- Stability: `stable`
- MCP tool: not exposed
- Result contract: `modules.list.v1`

Usage:

```bash
jurisd list-modules [--modules-dir DIR]
```

Arguments:

- none

Flags:

- `--modules-dir` (string): Override modules directory.

Examples:

- `jurisd list-modules [--modules-dir DIR]`

## `resolve-citation`

Resolve a citation to an authoritative source.

- Command id: `cite.resolve`
- Group: `cite`
- Side effect: `network_read`
- Stability: `stable`
- MCP tool: `resolve_citation`
- Result contract: `cite.resolve.v1`

Usage:

```bash
jurisd resolve-citation '[1992] HCA 23'
```

Arguments:

- `citation`: citation argument.

Flags:

- `--mode` (string): Resolution mode.
- `--format` (string): Output format.

Examples:

- `jurisd resolve-citation '[1992] HCA 23'`

## `search-cases`

Search Australian and New Zealand case law.

- Command id: `search.cases`
- Group: `search`
- Side effect: `network_read`
- Stability: `stable`
- MCP tool: `search_cases`
- Result contract: `legal_search_results.v1`

Usage:

```bash
jurisd search-cases <query> [--jurisdiction cth] [--limit 10]
```

Arguments:

- `query`: Search query.

Flags:

- `--jurisdiction` (string): Jurisdiction code.
- `--limit` (number): Maximum result count.
- `--offset` (number): Pagination offset.
- `--format` (string): Output format.
- `--sort-by` (string): Sort mode.
- `--method` (string): Search method.

Examples:

- `jurisd search-cases "native title" --jurisdiction cth --limit 5`

## `search-citing-cases`

Search for cases citing a named case.

- Command id: `cite.searchCitingCases`
- Group: `cite`
- Side effect: `network_read`
- Stability: `stable`
- MCP tool: `search_citing_cases`
- Result contract: `cite.searchCitingCases.v1`

Usage:

```bash
jurisd search-citing-cases 'Mabo v Queensland (No 2)'
```

Arguments:

- `caseName`: caseName argument.

Flags:

- `--format` (string): Output format.

Examples:

- `jurisd search-citing-cases 'Mabo v Queensland (No 2)'`

## `search-legislation`

Search Australian and New Zealand legislation.

- Command id: `search.legislation`
- Group: `search`
- Side effect: `network_read`
- Stability: `stable`
- MCP tool: `search_legislation`
- Result contract: `legal_search_results.v1`

Usage:

```bash
jurisd search-legislation <query> [--jurisdiction cth] [--limit 10]
```

Arguments:

- `query`: Search query.

Flags:

- `--jurisdiction` (string): Jurisdiction code.
- `--limit` (number): Maximum result count.
- `--offset` (number): Pagination offset.
- `--format` (string): Output format.
- `--sort-by` (string): Sort mode.
- `--method` (string): Search method.

Examples:

- `jurisd search-legislation "family violence" --jurisdiction nsw`

## `semantic-search-local`

Run local semantic search over installed data modules.

- Command id: `search.semanticLocal`
- Group: `search`
- Side effect: `local_metadata_read`
- Stability: `stable`
- MCP tool: `semantic_search_local`
- Result contract: `search.semanticLocal.v1`

Usage:

```bash
jurisd semantic-search-local 'restraint of trade' --k 5
```

Arguments:

- `query`: query argument.

Flags:

- `--k` (number): k value.
- `--module` (string): Pinned data module name.
- `--filter-jurisdiction` (string): Jurisdiction facet filter.
- `--filter-type` (string): Document type facet filter.
- `--filter-segment-type` (string): Segment type facet filter.
- `--format` (string): Output format.

Examples:

- `jurisd semantic-search-local 'restraint of trade' --k 5`

## `verify-module`

Verify an installed data module.

- Command id: `modules.verify`
- Group: `modules`
- Side effect: `local_metadata_read`
- Stability: `stable`
- MCP tool: not exposed
- Result contract: `modules.verify.v1`

Usage:

```bash
jurisd verify-module <name> [--modules-dir DIR]
```

Arguments:

- `name`: name argument.

Flags:

- `--modules-dir` (string): Override modules directory.

Examples:

- `jurisd verify-module <name> [--modules-dir DIR]`

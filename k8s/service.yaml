---
apiVersion: v1
kind: Service
metadata:
  name: auslaw-mcp
  namespace: auslaw-mcp
  labels:
    app.kubernetes.io/name: auslaw-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: auslaw-mcp
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
